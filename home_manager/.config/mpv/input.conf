# mpv keybindings
#
# Location of user-defined bindings: ~/.config/mpv/input.conf
#
# Lines starting with # are comments. Use SHARP to assign the # key.
# Copy this file and uncomment and edit the bindings you want to change.
#
# List of commands and further details: DOCS/man/input.rst
# List of special keys: --input-keylist
# Keybindings testing mode: mpv --input-test --force-window --idle
#
# Use 'ignore' to unbind a key fully (e.g. 'ctrl+a ignore').
#
# Strings need to be quoted and escaped:
#   KEY show-text "This is a single backslash: \\ and a quote: \" !"
#
# You can use modifier-key combinations like Shift+Left or Ctrl+Alt+x with
# the modifiers Shift, Ctrl, Alt and Meta (may not work on the terminal).
#
# The default keybindings are hardcoded into the mpv binary.
# You can disable them completely with: --no-input-default-bindings

# Developer note:
# On compilation, this file is baked into the mpv binary, and all lines are
# uncommented (unless '#' is followed by a space) - thus this file defines the
# default key bindings.

# If this is enabled, treat all the following bindings as default.
default-bindings start

MBTN_LEFT     ignore              # don't do anything
MBTN_LEFT_DBL cycle fullscreen    # toggle fullscreen
MBTN_RIGHT    cycle pause         # toggle pause/playback mode

## Seek units are in seconds, but note that these are limited by keyframes
RIGHT seek  5                          # seek 5 seconds forward
LEFT  seek -5                          # seek 5 seconds backward
UP    seek  60                         # seek 1 minute forward
DOWN  seek -60                         # seek 1 minute backward
Shift+RIGHT add time 85                 # skip forward 85 seconds
Ctrl+LEFT   no-osd sub-seek -1         # seek to the previous subtitle
Ctrl+RIGHT  no-osd sub-seek  1         # seek to the next subtitle
PGUP add chapter 1                     # seek to the next chapter
PGDWN add chapter -1                   # seek to the previous chapter
ESC set fullscreen no                  # leave fullscreen

. frame-step                           # advance one frame and pause
, frame-back-step                      # go back by one frame and pause
SPACE cycle pause                      # toggle pause/playback mode

z add sub-delay -0.1                   # shift subtitles 100 ms earlier
Z add sub-delay +0.1                   # delay subtitles by 100 ms
ctrl+z add audio-delay  0.100          # change audio/video sync by delaying the audio
ctrl+Z add audio-delay -0.100          # change audio/video sync by shifting the audio earlier
x add sub-scale +0.1                   # increase the subtitle font size
X add sub-scale -0.1                   # decrease the subtitle font size
9 add volume -2
0 add volume 2
m cycle mute                           # toggle mute


ctrl+< cycle deband                    # toggle the debanding filterc
ctrl+> cycle deinterlace               # cycle the deinterlacing filter
< add sub-pos -1                       # move subtitles up
> add sub-pos +1                       # move subtitles down
ctrl+< cycle sub-visibility            # hide or show the subtitles
c cycle sub                            # switch subtitle track
C cycle sub down                       # switch subtitle track backwards
v cycle audio                          # switch audio track

f cycle fullscreen                     # toggle fullscreen
s screenshot                           # take a screenshot of the video in its original resolution with subtitles
S screenshot video                     # take a screenshot of the video in its original resolution without subtitles
A cycle-values video-aspect-override "16:9" "4:3" "2.35:1" "-1" # cycle the video aspect ratio ("-1" is the container aspect)
#g ignore

#
# Not assigned by default
# (not an exhaustive list of unbound commands)
#